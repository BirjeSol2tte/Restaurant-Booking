spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/restaurant_booking
    username: postgres
    password: qwerty
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true